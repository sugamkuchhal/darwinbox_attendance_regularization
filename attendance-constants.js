const ATTEMPTS_PER_REASON = 2;
const MODAL_OPEN_TIMEOUT_MS = 5000;
const UI_SLEEP_SHORT_MS = 300;
const UI_SLEEP_MENU_MS = 800;
const UI_SLEEP_RETRY_MS = 500;
const UI_SLEEP_SUBMIT_MS = 3000;

module.exports = {
  ATTEMPTS_PER_REASON,
  MODAL_OPEN_TIMEOUT_MS,
  UI_SLEEP_SHORT_MS,
  UI_SLEEP_MENU_MS,
  UI_SLEEP_RETRY_MS,
  UI_SLEEP_SUBMIT_MS,
};
